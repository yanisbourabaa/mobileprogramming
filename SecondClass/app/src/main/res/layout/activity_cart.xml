<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_cart"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:clipToPadding="false"
    tools:context="com.example.yanisbourabaa.secondclass.cart.CartActivity">

    <TextView
        android:id="@+id/cart_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="32dp"
        android:paddingBottom="8dp"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:textStyle="bold"
        android:background="@color/colorPrimary"
        android:textColor="@android:color/white"
        android:elevation="8dp"
        android:text="You still can change some things\nif you want..."/>
    <FrameLayout
        android:id="@+id/cart_container"
        android:layout_below="@id/cart_text"
        android:layout_above="@+id/cart_btn_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <LinearLayout
        android:id="@+id/cart_btn_container"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:elevation="8dp"
        android:background="@android:color/white">
        <Button
            android:id="@+id/cart_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/BlueButton"
            android:text="Check out"
            android:background="@drawable/blue_btn" />
    </LinearLayout>


</RelativeLayout>
